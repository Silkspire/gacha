from tkinter import *
from PIL import ImageTk, Image
import os

character_path = "img/characters/"
monster_path   = "img/monsters/"
weapon_path    = "img/weapons/"
input_path     = "img/src/"

# root = Tk()
# img = ImageTk.PhotoImage(Image.open('img/characters/tmp_mdtvdek.png'))
# panel = Label(root, image=img)
# panel.pack(side = "bottom", fill = "both", expand = "yes")
# root.mainloop()



# for root, dirs, files in os.walk(character_path, topdown=False):
#     for name in files:
#         print(os.path.join(root, name))



#db.instantiate_character(base_character_id, owner_id)